def init_services(app_context):
    pass
    # Creating a pymongo client
    #mongo = MongoClient('localhost', 27017)
    #app_context.extensions['mongo.datasource'] = mongo['epicml']
#
    #DataService(app_context)
    #SelectedValuesService(app_context)
